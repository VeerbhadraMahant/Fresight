{
  "name": "freightsight-edge",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object: viewer-gated live AIS relay for the FreightSight map",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
